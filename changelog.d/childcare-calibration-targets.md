- Share the childcare calibration targets between the take-up optimisation and the test that checks them, so the two cannot drift apart. The test had kept the 0.6 / 660 Tax-Free Childcare targets from the September 2024 release after the optimisation moved to 0.63 / 985. Its tolerance also allowed any ratio in (0, 2), so it could not detect a doubling — TFC spending was passing at 1.96x its target. The tolerance is now ±40%, with TFC spending recorded as a known miss so the gap is reported rather than hidden.
