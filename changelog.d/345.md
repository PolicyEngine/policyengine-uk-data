Add the full plumbing for per-year panel snapshots: panel ID contract, `create_yearly_snapshots` helper, `age_dataset` demographic ageing module, year-aware loss matrices with a documented `resolve_target_value` fallback policy, cross-year smoothness penalty on `calibrate_local_areas`, a private-destination-locked `upload_yearly_snapshots` uploader and an `enhanced_frs_for_year` test-fixture factory (#345).
